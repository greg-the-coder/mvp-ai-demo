{
  "name": "env-dashboard-infra",
  "version": "1.0.0",
  "description": "AWS CDK infrastructure for Environment Status Dashboard",
  "scripts": {
    "build": "tsc",
    "synth": "cdk synth",
    "deploy": "cdk deploy --require-approval never",
    "destroy": "cdk destroy --force"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.2",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "typescript": "~5.6.3",
    "ts-node": "^10.9.2",
    "@types/node": "^22.10.0"
  }
}
